import styled from '@emotion/styled';

const Panel = styled.div`
  position: relative;
  display: grid;
  justify-content: center;
  align-items: center;
  margin: 0 auto;
  min-height: 100vh;
`;

export default Panel;
